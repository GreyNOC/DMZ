# Local lab

The local lab runs the GreyNOC DMZ app in Docker and mounts `reports/` and `evidence/` so generated files remain on the host.

Start from the repo root:

```bash
docker compose up --build
```
