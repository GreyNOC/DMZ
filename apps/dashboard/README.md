# Dashboard

The dashboard is served by the Python app for now. It shows scenario pass/fail status, fired rules, and missing rules.

Start it with:

```bash
greynoc-dmz dashboard --host 127.0.0.1 --port 8787
```
