# Encoded PowerShell execution

An encoded PowerShell command line was observed. Decode the `-enc` payload in a sandbox, identify the parent process, and confirm whether the activity was an approved admin task.
